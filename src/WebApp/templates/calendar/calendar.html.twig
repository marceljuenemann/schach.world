{% extends 'layout.html.twig' %}

{% set title = "Terminkalender" %}

{% block content %}
<ul>
  {% for event in events %}
    <li>{{ event.date }} {{ event.name }}</li>
  {% endfor %}
</ul>
{% endblock %}
