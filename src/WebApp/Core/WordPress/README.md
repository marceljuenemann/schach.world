# Nsv\WebApp\Core\WordPress

This namespace contains code that glues our Symfony webapp and WordPress together.

Ideally, the rest of the code base should not depend on WordPress directly, so that we could easily migrate the code to run without WordPress entirely.
